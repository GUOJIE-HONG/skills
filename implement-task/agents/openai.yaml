interface:
  display_name: "Implement Task"
  short_description: "Run independent tasks in parallel by dependency order"
  default_prompt: "Use $implement-task on this feature directory to run independent ready tasks in parallel isolated worktrees, then integrate and validate them before starting dependent tasks."
policy:
  allow_implicit_invocation: false
