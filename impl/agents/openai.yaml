interface:
  display_name: "Impl"
  short_description: "Implement a spec or tickets in this session, following design.md"
  default_prompt: "Use $impl to implement this spec or ticket in the current session, following the direction recorded in design.md."
policy:
  allow_implicit_invocation: false
