interface:
  display_name: "Prompt to New Session"
  short_description: "Hand current progress to another session as one prompt"
  default_prompt: "Use $ptns to hand the current progress to a new session."
policy:
  allow_implicit_invocation: false
