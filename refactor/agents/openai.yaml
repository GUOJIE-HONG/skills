interface:
  display_name: "Refactor"
  short_description: "Refactor under a plan, test-first when behavior changes"
  default_prompt: "Use $refactor to classify this refactor as behavior-preserving or behavior-changing, plan it, and carry it out."
policy:
  allow_implicit_invocation: false
